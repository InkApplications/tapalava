{
    "links": {
        "self": "{{ url('events-read', {schedule: schedule.id, _format: "json"}) }}",
        "schedule": "{{ url('schedule-read', {schedule: schedule.id, _format: "json"}) }}"
    },
    "data": {{ events | serialize('json') | raw }}
}
