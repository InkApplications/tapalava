{
    "links": {
        "self": "{{ url('schedule-read', {schedule: schedule.id, _format: "json"}) }}",
        "events": "{{ url('events-read', {schedule: schedule.id, _format: "json"}) }}"
    },
    "data": {{ schedule | serialize('json') | raw }}
}
